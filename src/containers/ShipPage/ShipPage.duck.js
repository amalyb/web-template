// Placeholder duck file for ShipPage
// Currently, the page fetches data directly from the API endpoint
// If state management is needed in the future, it can be added here

const initialState = {};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

